"use strict"

// Declaro variable como tipo number
let edad = 35;

let cantidad = "100";

// Pasar string a number
let nuevaCantidad = Number(cantidad);

// Pasar string a int
let nuevoInt = parseInt(cantidad);

// Pasar string a float
let nuevoFloat = parseFloat(cantidad);