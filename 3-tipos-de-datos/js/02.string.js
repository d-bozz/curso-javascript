"use strict"

// Declaro una variable de tipo string con comillas dobles
var bebida = "agua";

// Declaro una variable de tipo string con comillas simples (esta es la forma mas correcta en javascript)
var comida = 'ceviche';

// Declaro una variable de tipo string con comillas dentro
var instrucción = "El platillo se llama 'ceviche' ";


var edad = 34;

//Convertimos un number a texto
var edadtxt = String(edad)